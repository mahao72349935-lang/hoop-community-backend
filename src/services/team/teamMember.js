// 成员相关逻辑：加人、踢人、改角色、通过邀请码加入等
// 目前先留空，等对应需求再写

// 举例未来要加：
// exports.joinByCode = async ({ userId, inviteCode }) => { ... };
// exports.kickMember = async ({ userId, teamId, targetUserId }) => { ... };
// exports.changeRole = async ({ userId, teamId, targetUserId, role }) => { ... };

module.exports = {};
